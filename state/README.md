## This folder contains state management from easy-peasy

Note that we didn't use it as much as we should've, because we never got around to refactoring state and tokens outside of react state management.
